# -*- mode: snippet -*-
# name: nasm-template
# key: <nasm
# --

extern printf
section .data
$0
section .bss
section .text
global main
main:
        ; prologue
        push rbp
        mov rbp, rsp

        ; epilogue
        leave
        ret